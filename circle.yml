machine:
  post:
    - pyenv global 3.5.2

dependencies:
  pre:
    - pip install -r requirements.txt

test:
  override:
    - py.test tests/
    # runs coverage
    - py.test --cov=pysnips tests/
    - coverage xml
    - python-codacy-coverage -r coverage.xml
    - codecov
